[package]
name = "tick_prediction"
version = "0.1.0"
description = "State-only prediction pools for Tick"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "tick_prediction"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "1.1.2"
